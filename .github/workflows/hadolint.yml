name: hadolint

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  hadolint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.4.0
      - uses: brpaz/hadolint-action@v1.5.0
